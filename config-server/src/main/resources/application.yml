server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:///C:/Users/USER/OneDrive/Desktop/Tamizh/Projects/quickly/config-repo

logging:
  level:
    org.springframework.cloud.config: INFO
